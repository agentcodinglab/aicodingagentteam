#!/usr/bin/env bash
# codex stub binary for CI end-to-end host driver tests (direction C, ADR-0018).
# Mimics `codex exec --skip-git-repo-check <prompt>`: prints a fixed artifact-bearing
# message to stdout and exits 0. No API key, no network.
set -euo pipefail

# Accept any args; we only care that stdout has content and exit 0.
# Real codex exec args: exec --skip-git-repo-check <prompt...>
cat <<"STUB"
[codex-stub] task received at $(date -u +%FT%TZ)
Generated artifact: src/generated/stub_output.go
package main
// stub-generated by codex stub binary (CI e2e)
func main() {}
STUB
